# Ontology / RDF pipeline
rdflib==7.6.0
pyshacl==0.31.0

# Core dependencies
pandas==3.0.3
numpy==2.4.6
tqdm==4.67.3  # Progress bars

# PDF processing
pdfplumber==0.11.9
pypdf==6.12.2
reportlab==4.4.10

requests==2.34.2
beautifulsoup4==4.14.3
trafilatura==2.0.0
lxml_html_clean==0.4.4

# Data processing
scikit-learn==1.8.0
joblib==1.5.3
# networkx>=3.0

# Database
psycopg2-binary==2.9.11  # PostgreSQL
# pymongo>=4.5.0  # MongoDB
# neo4j>=5.0.0  # Neo4j

# API
fastapi==0.128.6
pydantic==2.12.5
uvicorn==0.40.0
redis==7.3.0
slowapi==0.1.9  # Rate limiting
python-dotenv==1.2.2  # Load .env for local dev (GROQ_API_KEY, DATABASE_URL, etc.)
python-json-logger==4.0.0  # Structured logging
orjson==3.11.7  # Fast JSON parsing for Redis cache (optional, falls back to json if not available)

# Database Encryption (optional - falls back to regular SQLite if not available)
# Note: pysqlcipher3 requires SQLCipher system library
# If SQLCipher is not available, the system will use regular SQLite
# pysqlcipher3>=1.1.0  # Uncomment if you have SQLCipher installed

# Visualization (if needed)
# matplotlib>=3.7.0
# plotly>=5.15.0

# ML/NLP Processing (optional - system works without these)
# These are LARGE packages (torch ~2GB, transformers ~500MB) and slow down builds
# Uncomment only when you're ready to use ML features
# See requirements-ml.txt for ML dependencies
# sentence-transformers>=2.2.0
# spacy>=3.7.0
# NER ingest stack (pinned to known-good versions from 2026-06-01)
stanza==1.12.1
transformers==5.9.0
torch==2.12.0
