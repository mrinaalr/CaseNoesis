# ML/NLP Processing Dependencies
# Install these separately when ready to use ML features:
#   pip install -r requirements-ml.txt
#
# Note: These are LARGE packages that significantly slow down builds:
#   - torch: ~2GB
#   - transformers: ~500MB
#   - sentence-transformers: ~200MB
#   - spacy: ~100MB
#
# The system gracefully degrades if these are not installed.

sentence-transformers>=2.2.0
spacy>=3.7.0
transformers>=4.30.0
torch>=2.0.0
